#!/bin/bash

# This script start venv and installs required required packages
# Notice that Tensorflow is already installed on the machine, no need to reinstall

source ~/tensorflow/bin/activate
pip install numpy
